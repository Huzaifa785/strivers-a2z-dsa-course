#include <bits/stdc++.h>
using namespace std;

int main()
{

    /*
    these are the main data types we'll be using throughout:
    1. int - could store integer values up to certain limit
    2. string - used for string values (ex. "hey")
    3. char - used to store a single character (ex. "h")
    4. float - used to store decimal values
    5. double - used to store decimal values, greater range than float
    6. long -  could store integer values up to certain limit, greater range than int
    7. long long -  could store integer values up to certain limit, greater range than long

    note: refer notes of this day for accurate ranges
    */

    int x = 9, y = 10;
    string fruit = "Water Melon";
    char letter = 'a';
    float num1 = 7.8;
    // and so on...

    cout << x << endl
         << y << endl
         << fruit << endl
         << letter << endl
         << num1 << endl;

    // yeah so this all about for this chapter, let's move on to if else

    return 0;
}